# Anke Sports · 客户端状态

更新：2026-09-13。完整当前结论以[工作区 STATE](../STATE.md)为准，[整体计划](<../anke-sports 文档/Anke_Sports_实施计划.md>)规定后续顺序。

Web现有版本已部署，保留现有视觉。P0客户端修正已发布到开发环境公网：登录用户默认显示“我的关注”，按账号记忆主动选择的“全部赛事”；空范围会查询随后180天并显示下一场或明确覆盖范围；保存遇到revision冲突时保留草稿、读取最新版并重新计算。用户已于2026-09-13取消Chrome扩展及商店发布；既有扩展文件只保留历史本地证据，不再作为v1交付或支持入口。

## 验收边界

公网真实账号的最新用户反馈是已取消F1；本轮没有读取或修改该账号，不沿用旧的F1+马刺+利物浦快照作为当前配置。浏览器本地体验已复现根因：处于“全部赛事”时，取消关注后仍会看到公开F1赛程；切换“我的关注”后F1为0场。修正版代码现已部署，但没有冒充公网登录账号复测。个人Feed保留历史事件与Web当前关注筛选是不同边界。

## Git 与运行

本地验证通过：Next类型检查与生产构建、Worker 3项转发测试、`git diff --check`。取消Chrome前的扩展类型检查与30项协议/安全测试也曾通过，但已从默认CI和发布范围移除。Playwright在本地SQL体验验证默认个人筛选、主动选择全部后的刷新记忆、保存冲突自动重算、取消F1后“我的关注”赛车0场，以及1440×1000、1280×800、1024×800三档无横向溢出；见[P0浏览器证据](output/playwright/p0-calendar-experience-2026-09-13.md)。

客户端基线`13ca8b7`已发布为Cloudflare Worker版本`a4580a3e-3781-40be-b270-1dc80ee28a95`。公网日历HTTP 200并在真实浏览器加载51场公开比赛；status为200/no-store，匿名个人日历为401/no-store，新空状态文案已从公网静态包读回。详见[P0发布记录](worker/deployment-2026-09-13.md)。没有push；本次也没有重发或新建Azure资源。

历史过程保存在[整理前快照](<../anke-sports 文档/archive/2026-09-12-status-before-consolidation/>)；不重复放进当前状态。

最新目标为完整v1上线，客户端范围收敛为桌面Web，分发集成只做公网MCP；公共订阅于2026-09-14移出v1，订阅页只展示登录后的个人日历。官方直播和账号生命周期仍在上线计划，Google直连继续延期。

2026-09-13范围澄清：不做Skill，之前仅为使用场景讨论。公网MCP仍按既定计划交付；本次仅更新文档，无产品代码修改或部署。

本次刷新核对HEAD `a4c2a24`；运行Web代码证据仍为`13ca8b7`对应Worker。P0主要代码不再重复列为待开发，剩余关注Feed发布及设备读回；本轮未构建或部署。

## 2026-09-13 P0/P1 UI 与交互收口

当前客户端工作树已完成本轮审核的 P0/P1：周视图按当前时区滚到最早比赛附近；173 条待确认队列改为每页 20 条，增加搜索、创作者筛选、排序、按比赛分组、整页选择与批量确认/忽略，并将全量轮询从 10 秒降为前台 30 秒且内容未变时不替换状态。认证恢复期间使用明确加载态；增加跳到主要内容、减少侧栏重复焦点、补充搜索标签；账号行改为先打开账号面板；时区选择明确为临时显示；入口改称“MCP 与连接”。此前公共日历能力占位已按2026-09-14产品决策删除。

## 2026-09-14 移除公共订阅入口

订阅页仅保留登录后的个人日历，公共订阅组件、专用样式和无用客户端类型已删除。官方转播仍由赛事详情与个人Feed链路读取，与公共订阅组件无依赖。云端公共Feed代码保留，白名单继续为空；未来仅在明确的免登录分享或合作分发需求出现后重新启用。客户端提交`722edc7`已从干净提交`ff37f9d`发布为Worker版本`5c7c9bc3-6585-4601-9b88-703e806ca926`；公网浏览器确认没有公共订阅界面或`/api/v1/public-feed`请求，保留路由读回`unavailable`。见[发布记录](worker/public-subscription-removal-deployment-2026-09-14.md)。

视觉与动效按品牌规范补齐事件/日程/详情时间层级、44px 图标与按钮目标、15px 按钮文字及小字对比度；Modal、Drawer、Toast 具备退出态，按压反馈有独立时长，减少动态模式保留短透明度反馈并去除位移/缩放。`npm run typecheck`、`npm run build`、`git diff --check`通过；Playwright 已验证真实公开赛程渲染、00:30 周视图 `scrollTop=0`、173 条 mock 队列仅渲染 20 张卡片/9 页、筛选与 20 条批量 POST、认证无游客闪烁、账号确认面板/焦点恢复、公共日历不可用态及关键字号尺寸。详见[本地验收记录](output/playwright/p0-p1-ui-interaction-2026-09-13.md)。改动已提交为 `b671d05` 并部署为 Worker 版本 `7566f3a1-8a9c-4a69-b4b2-b0bfe2743bc2`；未 push。详见[发布记录](worker/ui-deployment-2026-09-13.md)。

## 2026-09-13 P2 品牌系统收拢

当前工作树新增根级 `tokens.css` 与 `design.md`，把四份运行样式中的颜色、常用间距、圆角、线宽和动效值统一为语义 Token。主界面已分批切到石墨 canvas／surface／raised 层级与酸橙动作色；团队／赛事色仅保留为 Token 化的数据识别色。订阅平台卡片改为紧凑两行说明列表；各路由改用任务标题并删除重复口号；设置页残留的 Chrome 扩展入口措辞已移除。

本地 `npm run typecheck`、`npm run build` 与 `git diff --check`通过。Playwright CLI 在 1024×800、1280×800、1440×900 验证无横向溢出，日历、关注、创作者、订阅和设置页可渲染；键盘首个 Tab 命中跳转链接，订阅页为两行新结构且旧卡片为零。正文、辅助文字、强调色和必要边界的实算对比度为 4.25–16.78:1。P2 已提交为 `5d26a0a` 并部署为 Worker 版本 `e2628970-f7a3-4661-a935-ad474b1750c7`；公网订阅列表和日历已读回。详见[P2 验收记录](output/playwright/p2-ui-system-2026-09-13.md)与[发布记录](worker/ui-deployment-2026-09-13.md)。未 push。

## 2026-09-13 球队 Logo（本地待发布）

球队来源支持可选 Logo；关注侧栏、“关注球队”选择卡及比赛详情优先显示 Logo，加载失败时退回原球队缩写。日历比赛卡不显示球队 Logo，保持紧凑信息层级。提交`a5f5cf7`、`85f0af4`已发布为Worker版本`3676cf53-3ac8-47ed-abee-f9dec91533cb`；登录浏览器已验关注页与比赛详情真实Logo。见[发布记录](worker/team-logo-deployment-2026-09-13.md)。

## 2026-09-14 主日历范围收敛（本地待发布）

登录后的主日历固定读取“我的关注”，移除“全部赛事／我的关注”同级切换与账号级范围记忆，避免英超、NBA等高密度联赛把主日历变成完整赛程库；既有`all`本地偏好不再生效。未登录访客改为单选公开赛程，默认F1，也可选择英超或NBA中的一支球队；不提供整库“全部赛事”。客户端通过`source_id`让服务端完成赛事／球队过滤，完整赛程数据继续用于搜索和关注选择。尚未部署或做公网账号验收。

“我的关注”页同步收敛：F1等赛车系列赛保留直接关注，英超与NBA改为只承担球队目录的第一级；用户选择联赛后才展开对应球队，联赛按钮本身不写入关注。已保存的旧整联赛关注进入页面后成为待移除草稿，仍需用户通过预览确认才会修改账号。本地浏览器已验证初始隐藏球队、NBA只展开NBA球队、切换英超后只显示英足球队；类型检查与生产构建通过。尚未部署。

## 2026-09-14 我的关注交互审查（本地待发布）

访客无选择时保存按钮禁用；球队搜索只在展开某个联赛后出现；联赛入口持续显示已选球队数，切换联赛会清理旧搜索条件；登录前的选择在登录后自动合并账号原有关注并继续进入变更预览。联赛标题数字已从球队总数修正为可展开联赛数，搜索无匹配时有明确空状态。前后端直接关注规则统一为赛车赛事或非赛车球队，整 NBA／英超联赛及 F1 车队均不能绕过界面保存。

应用内浏览器已逐步检查首次进入、展开 NBA、选择湖人、切换英超、搜索空状态和登录边界；完整记录见[交互审查](output/playwright/following-flow-audit-2026-09-14.md)。客户端 Prettier、类型检查、生产构建与 `git diff --check` 通过；服务端 Ruff、最终38项定向测试、全量 `386 passed / 2 skipped` 与 `git diff --check` 通过。客户端已发布为Worker `a87681cd-476e-41b3-9e08-c287e8d25bc4`，后端最终OneDeploy为`14fb2ac0-4b07-446f-854a-77e5673274fb`。公网登录浏览器已读回两级球队目录、NBA 30支、原关注未变以及主日历仅显示关注比赛；见[发布记录](worker/calendar-following-deployment-2026-09-14.md)。未执行真实关注保存或外部日历刷新。
