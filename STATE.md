# Anke Sports · 客户端状态

更新：2026-09-13。完整当前结论以[工作区 STATE](../STATE.md)为准，[整体计划](<../anke-sports 文档/Anke_Sports_实施计划.md>)规定后续顺序。

Web现有版本已部署，保留现有视觉。P0客户端修正已发布到开发环境公网：登录用户默认显示“我的关注”，按账号记忆主动选择的“全部赛事”；空范围会查询随后180天并显示下一场或明确覆盖范围；保存遇到revision冲突时保留草稿、读取最新版并重新计算。用户已于2026-09-13取消Chrome扩展及商店发布；既有扩展文件只保留历史本地证据，不再作为v1交付或支持入口。

## 验收边界

公网真实账号的最新用户反馈是已取消F1；本轮没有读取或修改该账号，不沿用旧的F1+马刺+利物浦快照作为当前配置。浏览器本地体验已复现根因：处于“全部赛事”时，取消关注后仍会看到公开F1赛程；切换“我的关注”后F1为0场。修正版代码现已部署，但没有冒充公网登录账号复测。个人Feed保留历史事件与Web当前关注筛选是不同边界。

## Git 与运行

本地验证通过：Next类型检查与生产构建、Worker 3项转发测试、`git diff --check`。取消Chrome前的扩展类型检查与30项协议/安全测试也曾通过，但已从默认CI和发布范围移除。Playwright在本地SQL体验验证默认个人筛选、主动选择全部后的刷新记忆、保存冲突自动重算、取消F1后“我的关注”赛车0场，以及1440×1000、1280×800、1024×800三档无横向溢出；见[P0浏览器证据](output/playwright/p0-calendar-experience-2026-09-13.md)。

客户端基线`13ca8b7`已发布为Cloudflare Worker版本`a4580a3e-3781-40be-b270-1dc80ee28a95`。公网日历HTTP 200并在真实浏览器加载51场公开比赛；status为200/no-store，匿名个人日历为401/no-store，新空状态文案已从公网静态包读回。详见[P0发布记录](worker/deployment-2026-09-13.md)。没有push；本次也没有重发或新建Azure资源。

历史过程保存在[整理前快照](<../anke-sports 文档/archive/2026-09-12-status-before-consolidation/>)；不重复放进当前状态。

最新目标为完整v1上线，客户端范围收敛为桌面Web，分发集成只做公网MCP；公共Feed、官方直播和账号生命周期仍在上线计划，Google直连继续延期。具体阶段与环境决策以整体计划为准。
