# Chrome Web Store — Anke Sports

更新：2026-09-09。当前为本地开发包0.1.0；未创建商店项目、未上传、未提交审核。以下为对应实现的准备材料，不是已发布声明。

## 商店文案草案

名称：Anke Sports（本地安装包显示「Anke Sports · 本地」）。

短描述：查看近期体育赛程，将当前 YouTube 视频的原链接附到你确认的比赛。

详细描述：

查看未来7天的比赛，切换自己的关注与已接入的全部赛事，随时打开完整日历。

在 YouTube 视频页面打开扩展，主动读取视频标题和原链接，选择对应比赛和前瞻、复盘、直播或陪看类型，确认后保存到比赛。你之前移除的视频会继续保持隐藏。

连接 Anke Sports 账号并确认权限后使用。扩展只在你主动操作时读取当前视频；保存后由日历服务维护订阅源，系统日历显示时间取决于客户端刷新。

类别建议：Sports。主要语言：简体中文。单一用途：查看个人体育赛程并为比赛整理原始观看链接。

正式支持网站、联系邮箱、主页与隐私政策URL：待独立服务部署后填写真实地址。

## 图标与截图

| 资产 | 尺寸 | 当前状态 |
| --- | --- | --- |
| 原创图标 | 16/32/48/128 PNG | 构建时由 `extension/assets/icon.svg` 导出，尺寸检查通过 |
| 商店截图 | 1280×800 或640×400 | 尚未准备；独立合成预览不是实际安装证据 |
| 宣传素材 | 待确定 | 未创建 |

## 权限理由

| 权限 | 用途 |
| --- | --- |
| activeTab | 工具栏操作临时授权，用户再按读取按钮时取得当前 YouTube 视频标题与URL。没有后台浏览记录读取。 |
| storage | 会话内保留账号、赛程和视频草稿，让关闭弹窗后的继续操作可靠；本机保存公共注册和界面筛选。 |
| identity | 打开 Anke Sports 授权窗口并接收应用专属回调，让用户确认账号和权限。 |
| 本地 `http://localhost/*`；正式为构建时唯一API域名 | 读取已授权赛程、提交明确确认的链接、刷新及撤销账号连接。正式域名尚未配置，正式包必须重新核对权限。 |

不申请所有网站、tabs、history、scripting、后台 YouTube 域名、通知或内容脚本权限。扩展关闭后的创作者更新由独立服务端维护。实际 manifest 由 `extension/build.mjs` 生成；仅打包9个客户端文件和必要目录。

## 隐私与数据使用

披露范围包含账号身份标识/名称、授权凭证、个人赛程和关注相关结果，以及用户选定视频的URL/标题。即使不会记录浏览历史，用户主动提交的网页URL仍应如实填入商店对应数据类别，不能声明「不收集任何数据」。用途仅为上述单一功能。

凭证和草稿采用浏览器会话存储，不经Chrome同步；持久设置不含令牌。保存时视频信息发往已连接Anke Sports服务，无广告/分析/AI处理/远程代码。服务端数据、撤销、删除与备份保留需要纳入完整政策。草案：`extension/privacy.md`。正式填写 Dashboard 前核对当时的数据披露字段与已部署服务行为。

## 验证与发布状态

- 已完成：本地构建、PNG尺寸/ZIP内容、扩展单元边界测试、隔离服务端扩展来源/幂等/屏蔽/撤销测试、380×560合成界面检查。
- 尚未完成：实际Chrome安装、identity窗口与activeTab授权、真实账号/YouTube页面、休眠唤醒和生产回调、公开HTTPS服务、商店截图/隐私链接、商店项目和审核。
- 浏览器自动审批禁止打开扩展管理页；未通过其他方式安装。
- 正式包必须使用实际部署域名和商店身份，不能上传本地包或把合成预览当真实功能截图。

## 版本历史

0.1.0，2026-09-09：本地MV3赛程列表、短期账号连接与明确确认的视频附加流程；尚未发布。
